[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "strands-sql-session-manager"
version = "0.2.0"
description = "SQL (SQLAlchemy) session manager for Strands Agents — persists sessions, agent state, and messages"
authors = [{name = "Kamal", email = "skamalj@github.com"}]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "strands-session-sql>=0.2.0",
]
keywords = [
    "strands", "strands-agents", "session", "session-manager",
    "sql", "sqlalchemy", "postgresql", "mysql", "sqlite",
    "agent-state", "memory", "persistence",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

[project.urls]
Homepage = "https://github.com/skamalj/strands-agents-session"
Repository = "https://github.com/skamalj/strands-agents-session.git"
Documentation = "https://skamalj.github.io/agentstate-reducer/strands/providers/sql/"

[tool.hatch.build.targets.wheel]
packages = ["src/strands_sql_session_manager"]
